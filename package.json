{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Creation of a HTML portfolio.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "tailwindcss build -i ./public/css/tailwind.css -o ./public/css/style.css",
    "build:css:watch": "tailwindcss build -i ./public/css/tailwind.css -o ./public/css/style.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs-extra": "^11.1.1",
    "jsdom": "^22.1.0"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.2"
  }
}
